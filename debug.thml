<!doctype html><meta charset="utf-8"><title>debug</title>
<pre id="out"></pre>
<button onclick="['tp_token_v1','tp_redirect','tp_email'].forEach(k=>localStorage.removeItem(k));location.reload()">Borrar sesión</button>
<script>
  out.textContent = JSON.stringify({
    origin: location.origin,
    tp_token_v1: localStorage.getItem('tp_token_v1'),
    tp_redirect: localStorage.getItem('tp_redirect'),
    tp_email: localStorage.getItem('tp_email')
  }, null, 2);
</script>
